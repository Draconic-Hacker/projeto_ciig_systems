# Estrutura dos arquivos
```
src/
  ├── routes/
  │    ├── auth.routes.ts    (Login, Registro, Recuperar Senha)
  │    ├── user.routes.ts    (Perfil, Listagem de usuários)
  │    └── message.routes.ts (Envio de e-mails/mensagens)
  └── server.ts              (Apenas a configuração e ativação das rotas)
```